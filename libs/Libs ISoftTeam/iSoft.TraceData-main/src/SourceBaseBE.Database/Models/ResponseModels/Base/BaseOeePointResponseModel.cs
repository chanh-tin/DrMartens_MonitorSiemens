// -----------------------------------------------------------------------------
// This file was automatically generated.
// Please do not edit this file manually.
//
// Generated Date: 
//
// -----------------------------------------------------------------------------

using iSoft.Common.Utils;
using SourceBaseBE.Database.Entities;
using Microsoft.EntityFrameworkCore;
using SourceBaseBE.Database.ExtensionMethods;
using iSoft.Common.Models.ResponseModels;
using LinqKit;
using SourceBaseBE.Database.Enums;
using static iSoft.Common.ConstCommon;
using iSoft.Common.Enums;

namespace SourceBaseBE.Database.Models.ResponseModels
{
    public class BaseOeePointResponseModel : BaseCRUDResponseModel<OeePointEntity>
    {
        public virtual string? Name { get; set; }
        public virtual string? Note { get; set; }
        public virtual string? Description { get; set; }
        public virtual string? TagNames { get; set; }
        public virtual int? IdealRunRate { get; set; }
        public virtual int? IdealCycleTime { get; set; }
        public virtual long? OeePointConfigId { get; set; }
        public virtual OeePointConfigEntity? ItemOeePointConfig { get; set; }
        public virtual List<MachineBlockDataEntity>? ListMachineBlockData { get; set; }
        public virtual long? LineId { get; set; }
        public virtual LineEntity? ItemLine { get; set; }
        public virtual List<MachineEntity>? ListMachine { get; set; }
        
        public override object SetData(OeePointEntity entity)
        {
            base.SetData(entity);
            this.Name = entity.Name;
            this.Note = entity.Note;
            this.Description = entity.Description;
            this.TagNames = entity.TagNames;
            this.IdealRunRate = entity.IdealRunRate;
            this.IdealCycleTime = entity.IdealCycleTime;
            this.OeePointConfigId = entity.OeePointConfigId;
            this.ItemOeePointConfig = entity.ItemOeePointConfig;
            if (entity.ListMachineBlockData != null)
            {
                this.ListMachineBlockData = entity.ListMachineBlockData.Select(x => x).ToList();
            }
            this.LineId = entity.LineId;
            this.ItemLine = entity.ItemLine;
            if (entity.ListMachine != null)
            {
                this.ListMachine = entity.ListMachine.Select(x => x).ToList();
            }
        
            return this;
        }
        public override List<object> SetData(List<OeePointEntity> listEntity)
        {
            List<Object> listRS = new List<object>();
            foreach (OeePointEntity entity in listEntity)
            {
                listRS.Add(new BaseOeePointResponseModel().SetData(entity));
            }
            return listRS;
        }
        public IQueryable<OeePointEntity> PrepareWhereQueryFilter(
            IQueryable<OeePointEntity> query,
            Dictionary<string, object> dicInputFieldKey2Value,
            Func<Dictionary<string, string>> getFieldAttributes,
            Type responseType)
        {
            var dicFieldAttr = getFieldAttributes();
            foreach (var keyVal in dicInputFieldKey2Value)
            {
                string inputFieldKey = keyVal.Key;
                if (!dicFieldAttr.ContainsKey(inputFieldKey))
                {
                    continue;
                }

                var fieldType = dicFieldAttr[inputFieldKey];
                object? searchValue = ConvertUtil.ConvertData(keyVal.Value, fieldType);
                if (searchValue == null)
                {
                    continue;
                }

                var propertyInfo = responseType.GetProperty(inputFieldKey);
                if (propertyInfo == null)
                {
                    continue;
                }

                query = query.WherePropertyEquals(inputFieldKey, searchValue);
            }
            return query;
        }

        public IQueryable<OeePointEntity> PrepareQuerySort(
            IQueryable<OeePointEntity> query,
            Dictionary<string, long> dicInputFieldKey2SortOrder,
            Func<Dictionary<string, string>> getFieldAttributes,
            Type responseType)
        {
            var dicFieldAttr = getFieldAttributes();
            foreach (var keyVal in dicInputFieldKey2SortOrder)
            {
                string inputFieldKey = keyVal.Key;

                var propertyInfo = responseType.GetProperty(inputFieldKey);
                if (propertyInfo == null)
                {
                    continue;
                }

                if (keyVal.Value == -1)
                {
                    query = query.OrderByPropertyDescending(inputFieldKey);
                }
                else
                {
                    query = query.OrderByPropertyAscending(inputFieldKey);
                }
            }
            return query;
        }
        public IQueryable<OeePointEntity> PrepareWhereQuerySearch(
            IQueryable<OeePointEntity> query,
            Dictionary<string, string> dicInputFieldKey2Value,
            Func<Dictionary<string, string>> getFieldAttributes,
            Func<Dictionary<string, string>> getFieldAttributesSearchAll,
            Type responseType)
        {
            var dicFieldAttr = getFieldAttributes();
            var dicFieldAttrSearchAll = getFieldAttributesSearchAll();
            var predicate = LinqKit.PredicateBuilder.New<OeePointEntity>(true);
            bool searchAllFlag = false;
            foreach (var keyVal in dicInputFieldKey2Value)
            {
                string inputFieldKeyAll = keyVal.Key;
                if (inputFieldKeyAll.Trim().ToUpper() == "ALL")
                {
                    searchAllFlag = true;
                    string searchValue = ConvertUtil.GetString(keyVal.Value);
                    if (searchValue == null)
                    {
                        break;
                    }

                    foreach (var keyVal2 in dicFieldAttrSearchAll)
                    {
                        string inputFieldKey = keyVal2.Key;
                        predicate = this.GetPredicate(predicate, inputFieldKey, searchValue);
                    }
                    break;
                }
            }

            if (!searchAllFlag)
            {
                foreach (var keyVal in dicInputFieldKey2Value)
                {
                    string inputFieldKey = keyVal.Key;
                    if (!dicFieldAttr.ContainsKey(inputFieldKey))
                    {
                        continue;
                    }

                    var fieldType = dicFieldAttr[inputFieldKey];
                    string searchValue = ConvertUtil.GetString(keyVal.Value);
                    if (searchValue == null)
                    {
                        continue;
                    }

                    var propertyInfo = responseType.GetProperty(inputFieldKey);
                    if (propertyInfo == null)
                    {
                        continue;
                    }

                    predicate = this.GetPredicate(predicate, inputFieldKey, searchValue);
                }
            }
            return query.Where(predicate).AsQueryable();
        }

        public virtual ExpressionStarter<OeePointEntity> GetPredicate(ExpressionStarter<OeePointEntity> predicate, string inputFieldKey, string searchValue)
        {
            if (inputFieldKey == nameof(BaseOeePointResponseModel.Id))
            {
                predicate = predicate.Or(x => EF.Functions.Unaccent(x.Id.ToString().ToLower()).Contains(EF.Functions.Unaccent($"{searchValue.ToLower()}")));
            }
            else if (inputFieldKey == nameof(BaseOeePointResponseModel.Order))
            {
                predicate = predicate.Or(x => EF.Functions.Unaccent(x.Order.ToString().ToLower()).Contains(EF.Functions.Unaccent($"{searchValue.ToLower()}")));
            }
            return predicate;
        }

        public virtual Dictionary<string, string> GetFieldAttributes()
        {
            Dictionary<string, string> dic = new Dictionary<string, string>();
            
                    

            return dic;
        }

        public virtual Dictionary<string, string> GetFieldAttributesSearchAll()
        {
            Dictionary<string, string> dic = new Dictionary<string, string>();
            
                    

            return dic;
        }

        public virtual List<ColumnResponseModel> GetColumnAttribute()
        {
            List<ColumnResponseModel> columns = new List<ColumnResponseModel>();
            columns.Add(new ColumnResponseModel(
                nameof(BaseOeePointResponseModel.Id),
                nameof(BaseOeePointResponseModel.Id),
                true, true, true
                ));

            return columns;
        }
    }
}
