// -----------------------------------------------------------------------------
// This file was automatically generated.
// Please do not edit this file manually.
//
// Generated Date: 
//
// -----------------------------------------------------------------------------

using iSoft.Common.Utils;
using SourceBaseBE.Database.Entities;
using Microsoft.EntityFrameworkCore;
using SourceBaseBE.Database.ExtensionMethods;
using iSoft.Common.Models.ResponseModels;
using LinqKit;
using SourceBaseBE.Database.Enums;
using static iSoft.Common.ConstCommon;
using iSoft.Common.Enums;

namespace SourceBaseBE.Database.Models.ResponseModels
{
    public class BasePermissionDetailResponseModel : BaseCRUDResponseModel<PermissionDetailEntity>
    {
        public virtual string PermissionTable { get; set; }
        public virtual bool? View { get; set; }
        public virtual bool? Create { get; set; }
        public virtual bool? Edit { get; set; }
        public virtual bool? Delete { get; set; }
        public virtual bool? Request { get; set; }
        public virtual bool? Approve { get; set; }
        public virtual long? PermissionId { get; set; }
        public virtual PermissionEntity? ItemPermission { get; set; }
        
        public override object SetData(PermissionDetailEntity entity)
        {
            base.SetData(entity);
            this.PermissionTable = entity.PermissionTable;
            this.View = entity.View;
            this.Create = entity.Create;
            this.Edit = entity.Edit;
            this.Delete = entity.Delete;
            this.Request = entity.Request;
            this.Approve = entity.Approve;
            this.PermissionId = entity.PermissionId;
            this.ItemPermission = entity.ItemPermission;
        
            return this;
        }
        public override List<object> SetData(List<PermissionDetailEntity> listEntity)
        {
            List<Object> listRS = new List<object>();
            foreach (PermissionDetailEntity entity in listEntity)
            {
                listRS.Add(new BasePermissionDetailResponseModel().SetData(entity));
            }
            return listRS;
        }
        public IQueryable<PermissionDetailEntity> PrepareWhereQueryFilter(
            IQueryable<PermissionDetailEntity> query,
            Dictionary<string, object> dicInputFieldKey2Value,
            Func<Dictionary<string, string>> getFieldAttributes,
            Type responseType)
        {
            var dicFieldAttr = getFieldAttributes();
            foreach (var keyVal in dicInputFieldKey2Value)
            {
                string inputFieldKey = keyVal.Key;
                if (!dicFieldAttr.ContainsKey(inputFieldKey))
                {
                    continue;
                }

                var fieldType = dicFieldAttr[inputFieldKey];
                object? searchValue = ConvertUtil.ConvertData(keyVal.Value, fieldType);
                if (searchValue == null)
                {
                    continue;
                }

                var propertyInfo = responseType.GetProperty(inputFieldKey);
                if (propertyInfo == null)
                {
                    continue;
                }

                query = query.WherePropertyEquals(inputFieldKey, searchValue);
            }
            return query;
        }

        public IQueryable<PermissionDetailEntity> PrepareQuerySort(
            IQueryable<PermissionDetailEntity> query,
            Dictionary<string, long> dicInputFieldKey2SortOrder,
            Func<Dictionary<string, string>> getFieldAttributes,
            Type responseType)
        {
            var dicFieldAttr = getFieldAttributes();
            foreach (var keyVal in dicInputFieldKey2SortOrder)
            {
                string inputFieldKey = keyVal.Key;

                var propertyInfo = responseType.GetProperty(inputFieldKey);
                if (propertyInfo == null)
                {
                    continue;
                }

                if (keyVal.Value == -1)
                {
                    query = query.OrderByPropertyDescending(inputFieldKey);
                }
                else
                {
                    query = query.OrderByPropertyAscending(inputFieldKey);
                }
            }
            return query;
        }
        public IQueryable<PermissionDetailEntity> PrepareWhereQuerySearch(
            IQueryable<PermissionDetailEntity> query,
            Dictionary<string, string> dicInputFieldKey2Value,
            Func<Dictionary<string, string>> getFieldAttributes,
            Func<Dictionary<string, string>> getFieldAttributesSearchAll,
            Type responseType)
        {
            var dicFieldAttr = getFieldAttributes();
            var dicFieldAttrSearchAll = getFieldAttributesSearchAll();
            var predicate = LinqKit.PredicateBuilder.New<PermissionDetailEntity>(true);
            bool searchAllFlag = false;
            foreach (var keyVal in dicInputFieldKey2Value)
            {
                string inputFieldKeyAll = keyVal.Key;
                if (inputFieldKeyAll.Trim().ToUpper() == "ALL")
                {
                    searchAllFlag = true;
                    string searchValue = ConvertUtil.GetString(keyVal.Value);
                    if (searchValue == null)
                    {
                        break;
                    }

                    foreach (var keyVal2 in dicFieldAttrSearchAll)
                    {
                        string inputFieldKey = keyVal2.Key;
                        predicate = this.GetPredicate(predicate, inputFieldKey, searchValue);
                    }
                    break;
                }
            }

            if (!searchAllFlag)
            {
                foreach (var keyVal in dicInputFieldKey2Value)
                {
                    string inputFieldKey = keyVal.Key;
                    if (!dicFieldAttr.ContainsKey(inputFieldKey))
                    {
                        continue;
                    }

                    var fieldType = dicFieldAttr[inputFieldKey];
                    string searchValue = ConvertUtil.GetString(keyVal.Value);
                    if (searchValue == null)
                    {
                        continue;
                    }

                    var propertyInfo = responseType.GetProperty(inputFieldKey);
                    if (propertyInfo == null)
                    {
                        continue;
                    }

                    predicate = this.GetPredicate(predicate, inputFieldKey, searchValue);
                }
            }
            return query.Where(predicate).AsQueryable();
        }

        public virtual ExpressionStarter<PermissionDetailEntity> GetPredicate(ExpressionStarter<PermissionDetailEntity> predicate, string inputFieldKey, string searchValue)
        {
            if (inputFieldKey == nameof(BasePermissionDetailResponseModel.Id))
            {
                predicate = predicate.Or(x => EF.Functions.Unaccent(x.Id.ToString().ToLower()).Contains(EF.Functions.Unaccent($"{searchValue.ToLower()}")));
            }
            else if (inputFieldKey == nameof(BasePermissionDetailResponseModel.Order))
            {
                predicate = predicate.Or(x => EF.Functions.Unaccent(x.Order.ToString().ToLower()).Contains(EF.Functions.Unaccent($"{searchValue.ToLower()}")));
            }
            return predicate;
        }

        public virtual Dictionary<string, string> GetFieldAttributes()
        {
            Dictionary<string, string> dic = new Dictionary<string, string>();
            dic.Add(nameof(BasePermissionDetailResponseModel.PermissionTable), typeof(string).ToString());
            dic.Add(nameof(BasePermissionDetailResponseModel.PermissionId), typeof(long).ToString());
                    

            return dic;
        }

        public virtual Dictionary<string, string> GetFieldAttributesSearchAll()
        {
            Dictionary<string, string> dic = new Dictionary<string, string>();
            dic.Add(nameof(BasePermissionDetailResponseModel.PermissionTable), typeof(string).ToString());
            dic.Add(nameof(BasePermissionDetailResponseModel.PermissionId), typeof(long).ToString());
                    

            return dic;
        }

        public virtual List<ColumnResponseModel> GetColumnAttribute()
        {
            List<ColumnResponseModel> columns = new List<ColumnResponseModel>();
            columns.Add(new ColumnResponseModel(
                nameof(BasePermissionDetailResponseModel.Id),
                nameof(BasePermissionDetailResponseModel.Id),
                true, true, true
                ));

            return columns;
        }
    }
}
