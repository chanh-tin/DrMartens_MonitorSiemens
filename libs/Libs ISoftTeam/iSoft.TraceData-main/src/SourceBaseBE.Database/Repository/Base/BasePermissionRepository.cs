// -----------------------------------------------------------------------------
// This file was automatically generated.
// Please do not edit this file manually.
//
// Generated Date: 
//
// -----------------------------------------------------------------------------

using iSoft.Common;
using iSoft.Common.Exceptions;
using iSoft.Common.Models.RequestModels;

using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using iSoft.Redis.Services;
using SourceBaseBE.Database.DBContexts;
using SourceBaseBE.Database.Entities;
using iSoft.Database.Models;
using UserEntity = SourceBaseBE.Database.Entities.UserEntity;
using ISoftProjectEntity = SourceBaseBE.Database.Entities.ISoftProjectEntity;
using iSoft.Common.Models.ConfigModel.Subs;
using iSoft.Common.Utils;

using iSoft.Common.Enums;
using System.Collections.Generic;
using SourceBaseBE.Database.Models.RequestModels;
using SourceBaseBE.Database.Models.ResponseModels;
using PRPO.Database.Helpers;
using iSoft.Common.ExtensionMethods;
using iSoft.Database.Entities;

namespace SourceBaseBE.Database.Repository
{
    public class BasePermissionRepository : BaseCRUDRepository<PermissionEntity>
    {
        public BasePermissionRepository(CommonDBContext dbContext)
            : base(dbContext)
        {
        }
        public override string TableName
        {
            get { return "Permissions"; }
        }
        public override string GetName()
        {
            return nameof(BasePermissionRepository);
        }
        public override object GetDisplayField(PermissionEntity entity)
        {
            return entity.Name;
            
            return entity.Id;
        }
        public override List<FormSelectOptionModel> GetSelectData(string entityName, EnumAttributeRelationshipType category)
        {
            List<FormSelectOptionModel> rs = GetListWithNoInclude().Select(x => new FormSelectOptionModel(x.Id, GetDisplayField(x))).ToList();
            return rs;
        }

        /// <summary>
        /// GetById (@GenCRUD)
        /// </summary>
        /// <param name="Id"></param>
        /// <returns></returns>
        /// <exception cref="DBException"></exception>
        public override PermissionEntity? GetById(long id, bool isTracking = false)
        {
            try
            {
                PermissionEntity? result = null;
                var dataSet = _context.Set<PermissionEntity>();
                IQueryable<PermissionEntity> queryable;
                if (!isTracking)
                {
                    queryable = dataSet.AsNoTracking().AsQueryable();
                }
                else
                {
                    queryable = dataSet.AsQueryable();
                }
                queryable = queryable
                      .Include(entity => entity.ListPermissionDetail)
                      .Include(entity => entity.ListUser)
                      .Include(entity => entity.ListUserGroup)
                      
                      .Where(entity => entity.DeletedFlag != true && entity.Id == id);

                if (!isTracking)
                {
                    queryable = queryable.Select(entity => new PermissionEntity()
                    {
                        Id = entity.Id,
                        Order = entity.Order,
                        DeletedFlag = entity.DeletedFlag,
                        CreatedAt = entity.CreatedAt,
                        CreatedBy = entity.CreatedBy,
                        UpdatedAt = entity.UpdatedAt,
                        UpdatedBy = entity.UpdatedBy,
                        Name = entity.Name,
                        Description = entity.Description,
                        EnabledFlag = entity.EnabledFlag,
                        ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList(),
                        ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList(),
                        ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList(),
                        
                    });
                    result = queryable.FirstOrDefault();
                }
                else
                {
                    var entity = queryable.FirstOrDefault();
                    if (entity != null)
                    {
                        entity.ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList();
                        entity.ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList();
                        entity.ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList();
                        
                    };
                    result = entity;
                }

                return result;
            }
            catch (Exception ex)
            {
                throw new DBException(ex);
            }
        }
        /// <summary>
        /// GetList (@GenCRUD)
        /// </summary>
        /// <param name="pagingReq"></param>
        /// <returns></returns>
        /// <exception cref="DBException"></exception>
        public override List<PermissionEntity> GetList(PagingRequestModel pagingReq = null, bool isDirect = false, bool isTracking = false)
        {
            try
            {
                List<PermissionEntity>? result = null;
                string cacheKey = $"{cacheKeyList}";
                if (pagingReq != null)
                {
                    cacheKey = $"{cacheKeyList}:{pagingReq.GetKeyCache()}";
                }
                if (!isDirect && !isTracking)
                {
                    result = this.GetRedisData<List<PermissionEntity>>(cacheKey, null);
                }

                if (result == null)
                {
                    var dataSet = _context.Set<PermissionEntity>();
                    IQueryable<PermissionEntity> queryable;
                    if (!isTracking)
                    {
                        queryable = dataSet.AsNoTracking().AsQueryable();
                    }
                    else
                    {
                        queryable = dataSet.AsQueryable();
                    }
                    queryable = queryable
                                .Include(entity => entity.ListPermissionDetail)
                                .Include(entity => entity.ListUser)
                                .Include(entity => entity.ListUserGroup)
                                
                                .Where(entity => entity.DeletedFlag != true)
                                .OrderBy(entity => entity.Order).ThenBy(entity => entity.Id)
                                ;

                    if (pagingReq != null)
                    {
                        queryable = queryable
                                .Skip(pagingReq.GetSkip()).Take(pagingReq.GetLimit());
                    }
                    else
                    {
                        queryable = queryable
                                .Skip(0).Take(ConstCommon.ConstSelectListMaxRecord);
                    }

                    if (!isTracking)
                    {
                        queryable = queryable.Select(entity => new PermissionEntity()
                        {
                            Id = entity.Id,
                            Order = entity.Order,
                            DeletedFlag = entity.DeletedFlag,
                            CreatedAt = entity.CreatedAt,
                            CreatedBy = entity.CreatedBy,
                            UpdatedAt = entity.UpdatedAt,
                            UpdatedBy = entity.UpdatedBy,
                            Name = entity.Name,
                            Description = entity.Description,
                            EnabledFlag = entity.EnabledFlag,
                            ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList(),
                            ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList(),
                            ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList(),
                            
                        });
                        result = queryable.ToList();
                    }
                    else
                    {
                        result = queryable.ToList();
                        foreach (var entity in result)
                        {
                            entity.ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList();
                            entity.ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList();
                            entity.ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList();
                            
                        }
                    }

                    this.AddEntityCacheKey(GetName(), cacheKey, true);
                    this.SetRedisData(cacheKey, result, ConstCommon.ConstGetListCacheExpiredTimeInSeconds);
                }
                return result;
            }
            catch (Exception ex)
            {
                throw new DBException(ex);
            }
        }
        /// <summary>
        /// GetListFilter (@GenCRUD)
        /// </summary>
        /// <param name="pagingReq"></param>
        /// <returns></returns>
        /// <exception cref="DBException"></exception>
        public (List<PermissionEntity> listPermission, long totalRecord) GetListFilter(
            PagingFilterRequestModel request,
            bool isDirect = false,
            bool isTracking = false)
        {
            try
            {
                Dictionary<string, object> filterParams = StringToDictionaryHelper.ToStringAndObj(request.FilterStr);
                Dictionary<string, string> searchParams = StringToDictionaryHelper.ToDicOrString2(request.SearchStr, true);
                Dictionary<string, long> sortParams = StringToDictionaryHelper.ToStringLongTest(request.SortStr);

                List<PermissionEntity>? result = null;
                long totalRecord = 0;

                string cacheKey = $"{cacheKeyList}_ListFilter:";
                if (request != null)
                {
                    cacheKey += $"{request.GetKeyCache()}";
                }

                if (!isDirect && !isTracking)
                {
                    (result, totalRecord) = this.GetRedisData<(List<PermissionEntity>, long)>(cacheKey, (null, 0));
                }

                if (result == null)
                {
                    var dataSet = _context.Set<PermissionEntity>();
                    IQueryable<PermissionEntity> queryable;
                    IQueryable<PermissionEntity> queryableCountTotal;
                    if (!isTracking)
                    {
                        queryable = dataSet.AsNoTracking().AsQueryable();
                    }
                    else
                    {
                        queryable = dataSet.AsQueryable();
                    }
                    queryable = queryable
                                .Include(entity => entity.ListPermissionDetail)
                                .Include(entity => entity.ListUser)
                                .Include(entity => entity.ListUserGroup)
                                
                                .Where(entity => entity.DeletedFlag != true)
                                ;

                    if (request.DateFrom != null)
                    {
                        queryable = queryable.Where(entity => entity.UpdatedAt != null && entity.UpdatedAt.Value >= request.DateFrom);
                    }
                    if (request.DateTo != null)
                    {
                        queryable = queryable.Where(entity => entity.UpdatedAt != null && entity.UpdatedAt.Value <= request.DateTo);
                    }

                    var responseModel = new BasePermissionResponseModel();

                    queryable = responseModel.PrepareWhereQueryFilter(
                        queryable, 
                        filterParams,
                        responseModel.GetFieldAttributes,
                        responseModel.GetType());

                    queryable = responseModel.PrepareWhereQuerySearch(
                        queryable,
                        searchParams,
                        responseModel.GetFieldAttributes,
                        responseModel.GetFieldAttributesSearchAll,
                        responseModel.GetType());

                    queryableCountTotal = queryable;
                    totalRecord = queryableCountTotal.LongCount();

                    queryable = queryable.OrderBy(entity => entity.Order).ThenBy(entity => entity.Id);

                    queryable = responseModel.PrepareQuerySort(
                        queryable,
                        sortParams,
                        responseModel.GetFieldAttributes,
                        responseModel.GetType());

                    if (request != null)
                    {
                        queryable = queryable
                                .Skip(request.GetSkip()).Take(request.GetLimit());
                    }
                    else
                    {
                        queryable = queryable
                                .Skip(0).Take(ConstCommon.ConstSelectListMaxRecord);
                    }

                    if (!isTracking)
                    {
                        queryable = queryable.Select(entity => new PermissionEntity()
                        {
                            Id = entity.Id,
                            Order = entity.Order,
                            DeletedFlag = entity.DeletedFlag,
                            CreatedAt = entity.CreatedAt,
                            CreatedBy = entity.CreatedBy,
                            UpdatedAt = entity.UpdatedAt,
                            UpdatedBy = entity.UpdatedBy,
                            Name = entity.Name,
                            Description = entity.Description,
                            EnabledFlag = entity.EnabledFlag,
                            ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList(),
                            ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList(),
                            ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList(),
                            
                        });

                        result = queryable.ToList();
                    }
                    else
                    {
                        result = queryable.ToList();
                        foreach (var entity in result)
                        {
                            entity.ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList();
                            entity.ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList();
                            entity.ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList();
                            
                        }
                    }

                    this.AddEntityCacheKey(GetName(), cacheKey, true);
                    this.SetRedisData(cacheKey, (result, totalRecord), ConstCommon.ConstGetListCacheExpiredTimeInSeconds);
                }
                return (result, totalRecord);
            }
            catch (Exception ex)
            {
                throw new DBException(ex);
            }
        }
        public override List<PermissionEntity> GetListByListIds(List<long> Ids, bool isDirect = false, bool isTracking = false)
        {
            try
            {
                List<PermissionEntity>? result = null;
                string cacheKey = $"{cacheKeyListByListIds}:{EncodeUtil.MD5(string.Join(",", Ids))}";
                if (!isDirect && !isTracking)
                {
                    result = this.GetRedisData<List<PermissionEntity>>(cacheKey, null);
                }

                if (result == null)
                {
                    var dataSet = _context.Set<PermissionEntity>();
                    IQueryable<PermissionEntity> queryable;
                    if (!isTracking)
                    {
                        queryable = dataSet.AsNoTracking().AsQueryable();
                    }
                    else
                    {
                        queryable = dataSet.AsQueryable();
                    }
                    queryable = queryable
                                .Include(entity => entity.ListPermissionDetail)
                                .Include(entity => entity.ListUser)
                                .Include(entity => entity.ListUserGroup)
                                
                                .Where(entity => entity.DeletedFlag != true && Ids.Contains(entity.Id))
                                .OrderBy(entity => entity.Order).ThenBy(entity => entity.Id);

                    if (!isTracking)
                    {
                        queryable = queryable.Select(entity => new PermissionEntity()
                        {
                            Id = entity.Id,
                            Order = entity.Order,
                            DeletedFlag = entity.DeletedFlag,
                            CreatedAt = entity.CreatedAt,
                            CreatedBy = entity.CreatedBy,
                            UpdatedAt = entity.UpdatedAt,
                            UpdatedBy = entity.UpdatedBy,
                            Name = entity.Name,
                            Description = entity.Description,
                            EnabledFlag = entity.EnabledFlag,
                            ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList(),
                            ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList(),
                            ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList(),
                            
                        });
                        result = queryable.ToList();
                    }
                    else
                    {
                        result = queryable.ToList();
                        foreach (var entity in result)
                        {
                            entity.ListPermissionDetail = entity.ListPermissionDetail == null ? null : entity.ListPermissionDetail.Where(x => x.DeletedFlag != true).ToList();
                            entity.ListUser = entity.ListUser == null ? null : entity.ListUser.Where(x => x.DeletedFlag != true).ToList();
                            entity.ListUserGroup = entity.ListUserGroup == null ? null : entity.ListUserGroup.Where(x => x.DeletedFlag != true).ToList();
                            
                        }
                    }

                    this.AddEntityCacheKey(GetName(), cacheKey, true);
                    this.SetRedisData(cacheKey, result, ConstCommon.ConstGetListCacheExpiredTimeInSeconds);
                }
                return result;
            }
            catch (Exception dbEx)
            {
                throw new DBException(dbEx);
            }
        }
        public void Upsert(PermissionEntity entity, List<PermissionDetailEntity> permissionDetailChildren, List<UserEntity> userChildren, List<UserGroupEntity> userGroupChildren, long? userId = null)
        {
            try
            {
                if (entity.Id <= 0)
                {
                    // Insert
                    Insert(entity, permissionDetailChildren, userChildren, userGroupChildren, userId);
                }
                else
                {
                    // Update
                    Update(entity, permissionDetailChildren, userChildren, userGroupChildren, userId);
                }
                return;
            }
            catch (Exception ex)
            {
                throw new DBException(ex);
            }
        }
        public void Insert(PermissionEntity entity, List<PermissionDetailEntity> permissionDetailChildren, List<UserEntity> userChildren, List<UserGroupEntity> userGroupChildren, long? userId = null)
        {
            try
            {
                if (entity.Id > 0)
                {
                    throw new DBException($"Insert, Unexpected Id in entity, Id={entity.Id}");
                }
                else
                {
                    if (userId != null)
                    {
                        entity.CreatedBy = userId;
                    }
                    entity.CreatedAt = DateTime.Now;
                    entity.UpdatedBy = entity.CreatedBy;
                    entity.UpdatedAt = entity.CreatedAt;
                    entity.DeletedFlag = false;
                    entity.ListPermissionDetail = MergeChildrenEntity(entity.ListPermissionDetail, permissionDetailChildren);

                    entity.ListUser = MergeChildrenEntity(entity.ListUser, userChildren);

                    entity.ListUserGroup = MergeChildrenEntity(entity.ListUserGroup, userGroupChildren);
                    
                    _context.Set<PermissionEntity>().Add(entity);
                }
                var result = _context.SaveChanges();
                ClearCachedData();
                return;
            }
            catch (Exception ex)
            {
                throw new DBException(ex);
            }
        }
        public void Update(PermissionEntity entity, List<PermissionDetailEntity> permissionDetailChildren, List<UserEntity> userChildren, List<UserGroupEntity> userGroupChildren, long? userId = null)
        {
            try
            {
                if (entity.Id <= 0)
                {
                    throw new DBException("Update, Id not found in entity");
                }
                else
                {
                    if (userId != null)
                    {
                        entity.UpdatedBy = userId;
                    }
                    entity.UpdatedAt = DateTime.Now;
                    entity.ListPermissionDetail = MergeChildrenEntity(entity.ListPermissionDetail, permissionDetailChildren);

                    entity.ListUser = MergeChildrenEntity(entity.ListUser, userChildren);

                    entity.ListUserGroup = MergeChildrenEntity(entity.ListUserGroup, userGroupChildren);
                    
                    _context.Set<PermissionEntity>().Update(entity);
                }
                var result = _context.SaveChanges();
                ClearCachedData();
                return;
            }
            catch (Exception ex)
            {
                throw new DBException(ex);
            }
        }
    }
}
